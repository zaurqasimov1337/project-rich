export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="rounded-xl border bg-surface p-8 text-center shadow-sm">
        <h1 className="text-2xl font-bold text-primary">EduSphere</h1>
        <p className="mt-2 text-muted">Foundation ready — auth &amp; shell arrive in Phase 2.</p>
      </div>
    </main>
  );
}
